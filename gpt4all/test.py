from model import *

model = Model()

print(model.chat(input = "Explain AI to me: "))

print(model.chat(input = "What is the best usecase for it??"))

print(model.chat(input = "What is the best usecase for it??"))